/*import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { DisplayStubbleComponent } from './display-stubble.component';

describe('DisplayStubbleComponent', () => {
  let component: DisplayStubbleComponent;
  let fixture: ComponentFixture<DisplayStubbleComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ DisplayStubbleComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(DisplayStubbleComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/
